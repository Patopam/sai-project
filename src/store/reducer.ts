export const reducer = (currentAction, currentState) => {

    const {actio, payload } = currentAction;

    switch (actio) {
        case '':
            
            break;
    
        default:
            break;
    }
};